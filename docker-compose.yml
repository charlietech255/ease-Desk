version: "3.8"

services:
  easedesk:
    build: .
    container_name: easedesk
    restart: unless-stopped
    ports:
      - "8444:8444"
    volumes:
      - ./workspace:/home/easedesk
    environment:
      - EASEDESK_VNC_PASS=easedesk123
      - TZ=UTC
    shm_size: "1g"
